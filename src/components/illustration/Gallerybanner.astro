<a href="#princess-knight" class="frame preview-banner" data-tags="Digital" title="The Princess and the Knight">
  <img src="/images/Princess&Knight2026.png" alt="The Princess and the Knight" draggable="false"/>
  <div class="preview-overlay">View →</div>
</a>

  <a href="#Kobold" class="frame offset" data-tags="Digital" title="Kobold Artificer">
    <img src="/images/Kobold2026.png" alt="Kobold Artificer" draggable="false"/>
     <div class="offset-overlay">View →</div>
  </a>
  <a href="#piece1" class="frame" data-tags="Mixed ConceptArt" title="Whispers of the Canopy">
    <img src="/images/example1.jpg" alt="Whispers of the Canopy" draggable="false" />
  </a>
  <a href="#piece2" class="frame offset" data-tags="ConceptArt" title="Clockwork Moth">
    <img src="/images/example2.jpg" alt="Clockwork Moth" draggable="false" />
  </a>
  <a href="#piece7" class="frame" data-tags="Mixed" title="Lantern Spirits">
    <img src="/images/example7.jpg" alt="Lantern Spirits" draggable="false" />
  </a>

  <style>
  .preview-banner {
  position: relative;
  display: inline-block;
  overflow: hidden;
  border: 4px solid #d8c7ab;
  border-radius: 12px;
  transition: transform 0.3s ease;
  max-width: 120px;
  aspect-ratio: 1 / 1;
}

.preview-banner img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  object-position: center top;
  transition: transform 0.5s ease, filter 0.5s ease;
}

.preview-banner:hover img {
  transform: scale(1.05);
  filter: blur(2px) brightness(0.9);
}

.preview-overlay {
  position: absolute;
  bottom: 0;
  width: 100%;
  padding: 0.5rem 0;
  background: rgba(0, 0, 0, 0.5);
  color: #fff8f0;
  text-align: center;
  font-size: 0.9rem;
  opacity: 0;
  transition: opacity 0.3s ease;
  pointer-events: none;
}

.preview-banner:hover .preview-overlay {
  opacity: 1;
}

.frame.offset {
  position: relative;
  display: inline-block;
  overflow: hidden;
  border: 4px solid #d8c7ab;
  border-radius: 12px;
  transition: transform 0.3s ease;
  max-width: 120px;
  aspect-ratio: 1 / 1;
}

.frame.offset img {
  width: 100%;
  height: 100%;
  transition: transform 0.5s ease, filter 0.5s ease;
}

.frame.offset:hover img {
  transform: scale(1.05);
  filter: blur(2px) brightness(0.9);
}

.offset-overlay {
  position: absolute;
  bottom: 0;
  width: 100%;
  padding: 0.5rem 0;
  background: rgba(0, 0, 0, 0.5);
  color: #fff8f0;
  text-align: center;
  font-size: 0.9rem;
  opacity: 0;
  transition: opacity 0.3s ease;
  pointer-events: none;
}

.frame.offset:hover .offset-overlay {
  opacity: 1;
}
  </style>