<a href="#ux-piece0" class="frame preview-banner" data-tags="Research" title="Research Overview">
  <img src="/images/ux-placeholder-1.svg" alt="Research placeholder" draggable="false"/>
  <div class="preview-overlay">View →</div>
</a>

<a href="#ux-piece1" class="frame offset" data-tags="Wireframe" title="Wireframe Set">
  <img src="/images/ux-placeholder-2.svg" alt="Wireframe placeholder" draggable="false"/>
  <div class="offset-overlay">View →</div>
</a>

<a href="#ux-piece2" class="frame preview-banner" data-tags="Prototype" title="Prototype Flow">
  <img src="/images/ux-placeholder-3.svg" alt="Prototype placeholder" draggable="false" />
  <div class="preview-overlay">View →</div>
</a>

<a href="#ux-piece3" class="frame offset" data-tags="Usability" title="Usability Study">
  <img src="/images/ux-placeholder-4.svg" alt="Usability placeholder" draggable="false" />
  <div class="offset-overlay">View →</div>
</a>

<style>
.preview-banner {
  position: relative;
  display: inline-block;
  overflow: hidden;
  border: 4px solid #9b78b7;
  border-radius: 12px;
  transition: transform 0.3s ease;
  max-width: 140px;
  aspect-ratio: 1 / 1;
}

.preview-banner img { width: 100%; height: 100%; object-fit: cover; object-position: center top; transition: transform 0.5s ease, filter 0.5s ease; }
.preview-banner:hover img { transform: scale(1.06); filter: brightness(0.85); }
.preview-overlay { position: absolute; bottom: 0; width: 100%; padding: 0.5rem 0; background: rgba(10,25,60,0.7); color: #f0f8ff; text-align:center; font-size:0.9rem; opacity:0; transition:opacity 0.3s ease; pointer-events:none; }
.preview-banner:hover .preview-overlay { opacity:1; }
.frame.offset { position: relative; display:inline-block; overflow:hidden; border:4px solid #9b78b7; border-radius:12px; transition: transform 0.3s ease; max-width:140px; aspect-ratio:1/1; }
.frame.offset img { width:100%; height:100%; transition: transform 0.5s ease, filter 0.5s ease; }
.frame.offset:hover img { transform: scale(1.06); filter: brightness(0.85); }
.offset-overlay { position:absolute; bottom:0; width:100%; padding:0.5rem 0; background: rgba(10,25,60,0.7); color:#f0f8ff; text-align:center; font-size:0.9rem; opacity:0; transition:opacity 0.3s ease; pointer-events:none; }
.frame.offset:hover .offset-overlay { opacity:1; }
</style>
